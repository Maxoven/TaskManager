# 📦 ГОТОВЫЙ BACKEND ДЛЯ MYSQL

## Что внутри:

- ✅ Все файлы backend готовы к работе с MySQL
- ✅ Упрощенная версия (базовый функционал работает)
- ✅ SQL скрипт для создания таблиц

---

## 🚀 КАК УСТАНОВИТЬ:

### 1. Создай таблицы в MySQL

1. Зайди в phpMyAdmin
2. Выбери свою базу данных
3. Вкладка "SQL"
4. Скопируй ВЕСЬ текст из файла `database-init.sql`
5. Вставь и нажми "Выполнить"

### 2. Загрузи файлы на сервер

**Через FTP (FileZilla):**
1. Подключись к серверу
2. Загрузи ВСЮ папку в `/home/u3409264/taskmanager/backend/`

**Или через ISPmanager:**
1. Менеджер файлов
2. Перейди в `/home/u3409264/taskmanager/`
3. Загрузи все файлы

### 3. Создай файл .env

В папке backend создай файл `.env` со следующим содержимым:

```
DB_HOST=localhost
DB_USER=твой_mysql_пользователь
DB_PASSWORD=твой_пароль
DB_NAME=твоя_база_данных
JWT_SECRET=любая-длинная-случайная-строка-минимум-32-символа
PORT=3001
NODE_ENV=production
```

**ЗАМЕНИ:**
- `твой_mysql_пользователь` - имя пользователя БД
- `твой_пароль` - пароль от БД
- `твоя_база_данных` - имя базы данных

### 4. Установи зависимости

Через Shell-клиент:

```bash
cd /home/u3409264/taskmanager/backend
npm install
```

Подожди 1-2 минуты.

### 5. Запусти сервер

```bash
node server.js
```

Должно показать: `🚀 Сервер запущен на порту 3001`

Если видишь это - **BACKEND РАБОТАЕТ!** ✅

---

## 📁 Структура файлов:

```
backend/
├── config/
│   └── database.js          # Подключение к MySQL
├── middleware/
│   └── auth.js              # JWT аутентификация
├── routes/
│   ├── auth.js              # Регистрация и вход
│   ├── projects.js          # Управление проектами  
│   └── tasks.js             # Управление задачами
├── package.json             # Зависимости
├── server.js                # Главный файл
├── .env.example             # Пример .env
└── database-init.sql        # SQL для создания таблиц
```

---

## 🔧 Настройка автозапуска

### Через ISPmanager (рекомендуется):

1. **Node.js приложения** → **Создать**
2. Заполни:
   - Домен: `tm.vakulenko.club`
   - URL: `/api`
   - Папка: `/home/u3409264/taskmanager/backend`
   - Скрипт: `server.js`
   - Версия Node: 18.x
3. **Запустить**

---

## ✅ Проверка:

Открой в браузере: `http://tm.vakulenko.club/api`

Должен вернуть:
```json
{"message":"Task Manager API работает!"}
```

---

## 🐛 Если не работает:

```bash
# Проверь логи
cd /home/u3409264/taskmanager/backend
node server.js

# Смотри какая ошибка
```

Частые проблемы:
- ❌ Неправильный пароль в `.env`
- ❌ База данных не создана
- ❌ Таблицы не созданы (не выполнен SQL скрипт)
- ❌ Порт 3001 занят

---

## 📞 Следующий шаг:

После того как backend заработает:
1. Собери frontend (на своём компьютере)
2. Загрузи на сервер
3. Настрой проксирование
4. Готово! 🎉

---

**Удачи! Если застрянешь - пиши!**
